# AGENTS.md

本文件是 OptiRoute 的协作与开发方向约束，适用于人类开发者与 Coding Agent。

## 0. 优先阅读

在修改代码前，先阅读 `docs/tech/technical-design.md` 建立整体认知。

该文档按包和文件拆解核心函数，是代码结构、数据流、并发模型与安全边界的主索引。

**功能改动后必须同步更新该技术文档。**

## 1. 项目定位

OptiRoute 是一个面向延迟与稳定性优化的分布式四层反向代理系统。

核心角色：

- **Center**：控制面，负责 Edge 管理、拓扑汇总、密钥轮换与容量控制
- **Edge**：数据面与引导面，负责客户端引导、链路探测、选路、Token 校验和 L4 转发
- **Client Agent**：运行在用户侧，负责本地接入、探测与透传
- **Server Agent**：运行在源站侧，负责认证接入、解析真实 IP 并转发到上游

目标平台：

- Windows / Linux / macOS
- IPv4 / IPv6 双栈

## 2. 架构不变量

以下约束是项目骨架，修改前必须理解并保持。

### 2.1 Center 是控制面权威

- 所有 Edge 必须向 Center 注册
- Center 是 shared_secret 的生成、轮换和下发方
- Center 负责限制在线 Edge 数量与注册频率
- 拓扑和密钥状态以 Center 为准

### 2.2 Edge 是数据面，不是信任源

- 客户端探测结果不可信，必须校验范围
- 客户端 Token 只允许目标 Edge 验证
- Edge 的注册信息必须经过 Center 校验后才能进入拓扑

### 2.3 Client Agent 保持不透明

- Client Agent 只负责连接、探测和透传
- 它不应解析或理解拓扑语义、路由策略和 Token 内部结构
- 协议演进应尽量保持 Client Agent 的“不透明转发”特性

### 2.4 Server Agent 只做源站侧职责

- 校验 Edge 接入身份
- 解析 PPv2 真实 IP
- 按配置决定是否向上游注入 PPv2
- 不参与选路和拓扑决策

### 2.5 引导与数据面分离

- 引导连接负责下发探测列表并返回目标节点
- 业务连接负责 Token 校验和 L4 转发
- 两者可以复用端口，但协议判定必须严格、超时必须明确

## 3. 安全模型

### 3.1 资源与拒绝服务防护

任何面向不可信网络的入口必须具备：

- 连接超时
- 读写超时或消息大小上限
- 并发握手上限
- 按来源限速
- 明确的拒绝路径和日志

### 3.2 本地持久化安全

- 缓存中如包含 secret，文件权限必须最小化
- 磁盘写入不得阻塞控制面消息循环
- 进程退出前应冲刷必要状态，但不能用空状态覆盖有效缓存

## 4. 版本与兼容策略

项目尚未进入 STABLE 版本。

- 不承担旧版本向后兼容义务
- 协议、配置、Token 格式可以随版本演进
- 旧格式兼容代码应尽快移除，而不是长期保留
- 升级路径和降级路径需要明确文档说明

## 5. 配置管理约束

新配置项必须包含：

- 明确默认值
- 范围校验
- 文档说明

其他要求：

- 0 值具有特殊语义时，必须使用可区分“未配置”与“显式 0”的方式
- 配置变更必须同步所有角色文档和示例配置
- 安全相关开关默认值应选择更安全的一侧

## 6. 开发流程约束

### 6.1 先设计，后修改

- 先说明修改目标和影响范围
- 涉及协议、密钥、配置或安全模型时，必须先给出方案
- 未经确认不直接修改

### 6.2 小步提交逻辑

- 每个批次保持可编译
- 一个批次聚焦一个目标
- 构建错误优先清理，再继续后续工作

### 6.3 跨平台约束

- 不允许引入仅限单一操作系统的实现替代跨平台方案
- 不要删除已有 Windows 兼容逻辑

### 6.4 代码整理边界

- 只格式化本次修改的文件
- 不进行全仓库格式化、重命名或行尾批量转换
- 不顺手清理与当前任务无关的代码

### 6.5 文档同步

- 中英文文档应同步更新
- 功能改动后必须同步更新 `docs/tech/technical-design.md` 中对应模块的函数说明
- README、配置文档、协议说明必须保持一致
- AGENTS.md 本身也应随架构方向变化更新

## 7. 测试约束

- 安全、协议、并发修复必须有回归测试
- 测试优先使用本机回环或内存网络，不依赖公网
- 网络测试必须设置超时，禁止无限阻塞

每个改动批次完成后至少执行：

- 单元测试
- 竞态测试
- 静态检查
- Windows 交叉编译

## 8. 协作原则

- 保持透明：每批改动后报告完成项、验证结果和剩余风险
- 不擅自提交 Git
- 不擅自发布版本
- 对不确定事项先提问，不猜测后静默实施
